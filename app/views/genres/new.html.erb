<%= form_for @genre do |f| %>

  <div class="field">
    <%= f.label "genre_name" %>:
    <%= f.text_field :genre_name %></div>
  <div class="actions">
    <%= f.submit "Create" %>
  </div>
<% end %>
